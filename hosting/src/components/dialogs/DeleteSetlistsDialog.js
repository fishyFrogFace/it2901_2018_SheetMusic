import React, { Component } from 'react'

import { withStyles} from "material-ui";
import AsyncDialog from './AsyncDialog';

const styles = {
};

export class DeleteSetlistsDialog extends Component {
  render() {
    return (
      <AsyncDialog>

      </AsyncDialog>
    )
  }
}

export default withStyles(styles)(DeleteSetlistsDialog);
